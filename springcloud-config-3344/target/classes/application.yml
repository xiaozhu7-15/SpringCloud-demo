server:
  port: 3344

spring:
  application:
    name: springcloud-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/xiaozhu7-15/myspringcloud-config.git #github上的git仓库名字


