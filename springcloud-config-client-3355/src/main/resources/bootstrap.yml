spring:
  cloud:
    config:
      name: springcloud-config-client #需要从GitHub上读取的资源名称，注意没有yml后缀名
      profile: dev #本地访问你的配置项
      label: master
      uri: http://localhost:3344 #本微服务启动后先去找3344服务，通过SpringCloudConfig获取GitHub的服务地址